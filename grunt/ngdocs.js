'use strict';

module.exports = {
    options: {
        title: '<%= meta.pkg.name %>'
    },
    all: {
        src: ['<%= meta.src %>/app/*.js'],
        title: 'Source Code Documentation'
    }
};
