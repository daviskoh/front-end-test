'use strict';

module.exports = {
    tasks: {
        options: {
            filter: 'include',
            tasks: [
                'compass',
                'jshint',
                'serve'
            ]
        }
    }
};
