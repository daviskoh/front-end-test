'use strict';

module.exports = {
    dev: {
        options: {
            sassDir: '<%= meta.src %>/styles/scss',
            cssDir: '<%= meta.src %>/styles/css',
            specify: '<%= meta.src %>/styles/scss/core.scss'
        }
    }
};